from setuptools import setup, find_packages

setup(
    name="royal_clash",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "pygame",
        "customtkinter",
        "pillow"
    ]
) 